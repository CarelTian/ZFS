syntax = "proto3";

package calculator;

option go_package = "./calculatorpb";

// 定义计算请求
message CalcRequest {
  int32 num1 = 1;
  int32 num2 = 2;
}

// 定义计算响应
message CalcResponse {
  int32 result = 1;
}

// 计算服务
service Calculator {
  rpc Add(CalcRequest) returns (CalcResponse);
}
